# Docker 설치 확인 가이드

## 🤔 어디서 확인하나요?

**답변: 두 가지 방법이 있습니다!**

---

## 방법 1: NAS 웹 인터페이스에서 확인 (가장 간단) ✅

### UGREEN NAS 웹 인터페이스 사용

1. **브라우저에서 NAS 접속**
   ```
   http://NAS_IP
   ```
   예: `http://192.168.1.100`

2. **앱 센터 또는 App Center 열기**
   - 메인 화면에서 "앱 센터" 또는 "App Center" 클릭

3. **Docker 확인**
   - 설치된 앱 목록에서 "Docker" 또는 "Container Station" 찾기
   - 있으면 ✅ 설치됨
   - 없으면 ❌ 설치 필요

4. **Docker 설치 (없는 경우)**
   - "Docker" 또는 "Container Station" 검색
   - "설치" 버튼 클릭
   - 설치 완료 대기

**장점:**
- ✅ SSH 접속 불필요
- ✅ GUI로 쉽게 확인
- ✅ 설치도 간단

---

## 방법 2: SSH로 접속해서 확인 (고급)

### Windows에서 SSH 접속

#### A. Windows PowerShell 사용

1. **PowerShell 열기**
   - Windows 키 + X
   - "Windows PowerShell" 또는 "터미널" 선택

2. **SSH 접속**
   ```powershell
   ssh admin@NAS_IP
   ```
   예: `ssh admin@192.168.1.100`

3. **비밀번호 입력**
   - NAS 관리자 비밀번호 입력

4. **Docker 확인**
   ```bash
   docker --version
   docker-compose --version
   ```

#### B. Windows CMD 사용

1. **CMD 열기**
   - Windows 키 + R
   - `cmd` 입력 후 Enter

2. **SSH 접속**
   ```cmd
   ssh admin@NAS_IP
   ```
   (Windows 10/11에는 SSH가 기본 포함되어 있습니다)

#### C. PuTTY 사용 (SSH 클라이언트)

1. **PuTTY 다운로드 및 설치**
   - https://www.putty.org/ 에서 다운로드

2. **PuTTY 실행**
   - Host Name: `NAS_IP` (예: 192.168.1.100)
   - Port: `22`
   - Connection type: `SSH`
   - "Open" 클릭

3. **로그인**
   - Username: `admin`
   - Password: NAS 관리자 비밀번호

4. **Docker 확인**
   ```bash
   docker --version
   docker-compose --version
   ```

---

## 📋 단계별 확인 방법

### 1단계: NAS IP 주소 확인

**방법 A: NAS 웹 인터페이스에서**
- NAS 설정 > 네트워크에서 IP 확인

**방법 B: Windows에서**
```cmd
# CMD에서
ping NAS이름
# 또는
arp -a
```

### 2단계: Docker 설치 여부 확인

#### 웹 인터페이스에서:
- 앱 센터에서 "Docker" 또는 "Container Station" 확인

#### SSH에서:
```bash
docker --version
# 출력 예시: Docker version 20.10.x
```

### 3단계: Docker가 없으면 설치

#### 웹 인터페이스에서:
- 앱 센터에서 Docker 설치

#### SSH에서:
```bash
opkg update
opkg install docker docker-compose
```

---

## 🎯 추천 순서

### 초보자: 웹 인터페이스 사용 ✅

1. 브라우저에서 `http://NAS_IP` 접속
2. 앱 센터에서 Docker 확인/설치
3. File Station에서 docker-compose.yml 업로드
4. Container Station에서 실행

### 고급 사용자: SSH 사용

1. PowerShell에서 SSH 접속
2. Docker 확인/설치
3. docker-compose 실행

---

## 💡 실제 예시

### 예시 1: 웹 인터페이스 사용

```
1. 브라우저: http://192.168.1.100
2. 로그인: admin / 비밀번호
3. 앱 센터 열기
4. "Docker" 검색
5. 설치되어 있으면 ✅
6. 없으면 "설치" 클릭
```

### 예시 2: SSH 사용

```powershell
# PowerShell에서
ssh admin@192.168.1.100

# 비밀번호 입력 후
docker --version

# 출력: Docker version 20.10.x
# 있으면 ✅ 설치됨
```

---

## ❓ 자주 묻는 질문

### Q1: SSH가 뭔가요?
**A:** 원격으로 NAS에 접속해서 명령어를 실행하는 방법입니다.

### Q2: CMD를 켜야 하나요?
**A:** 
- 웹 인터페이스 사용: ❌ CMD 불필요
- SSH 사용: ✅ CMD 또는 PowerShell 필요

### Q3: 어느 방법이 더 쉬운가요?
**A:** 웹 인터페이스가 더 쉽습니다! ✅

### Q4: Docker가 없으면 어떻게 하나요?
**A:** 
- 웹 인터페이스: 앱 센터에서 설치
- SSH: `opkg install docker` 명령어로 설치

---

## 🚀 다음 단계

Docker가 설치되어 있으면:
1. ✅ docker-compose.yml 파일 업로드 확인
2. ✅ MongoDB 실행 (`docker-compose up -d`)
3. ✅ 백엔드 연결 설정

---

## 📝 요약

| 방법 | 어디서? | 난이도 | 추천 |
|------|--------|--------|------|
| 웹 인터페이스 | 브라우저 | ⭐ 매우 쉬움 | ✅ 권장 |
| SSH | CMD/PowerShell | ⭐⭐ 보통 | 고급 사용자 |

**가장 쉬운 방법: 브라우저에서 NAS 웹 인터페이스 접속!** ✅

