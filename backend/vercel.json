{
    "version": 2,
    "builds": [
        {
            "src": "dist/server.mongodb.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "dist/server.mongodb.js"
        }
    ],
    "env": {
        "NODE_ENV": "production"
    }
}