{
    "version": 2,
    "builds": [
        {
            "src": "src/server.mongodb.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/server.mongodb.ts"
        }
    ],
    "env": {
        "NODE_ENV": "production"
    }
}